 pipeline {
       agent any
       parameters {
           string(name: 'AWS_REGION', defaultValue: 'us-east-1', description: 'AWS Region')
           string(name: 'ASG_NAME', defaultValue: 'terraform-20240911103500987200000001', description: 'Auto Scaling Group Name')
       }
environment {
        PRIVATE_KEY_PATH = 'tasks.pem'
    }
       stages {
         /*   stage('Terraform Apply') {
               steps {
                   script {
                       withEnv(["AWS_REGION=${params.AWS_REGION}"]) {
                           sh 'terraform init'
                           sh 'terraform apply -auto-approve'
                       }
                   }
               }
           }*/
           stage('Ansible Configure') {
            steps {
                sh '''
                ansible-playbook -i ansible/dynamic_inventory.py ansible/playbook.yml --private-key=${PRIVATE_KEY_PATH}
                '''
            }
        }
    
       }
   }
